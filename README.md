# metapet

A tiny CLI for capturing pet-project ideas the moment they strike, then growing
them from a one-line seed into a buildable spec.

Ideas live **outside** this repository, in your own data directory — the repo
holds only the tool.
